@model List<LibraryManagementSystem.Entities.Concrete.Translator>
@{
}

@foreach (var item in Model)
{
	<div class="row">
		<div class="col-md-4">@item.TranslatorName</div>
		<div class="col-md-4">@item.Gender</div>
		<div class="col-md-4">@item.Email</div>
	</div>
}
